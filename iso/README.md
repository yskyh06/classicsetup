# Local Windows ISO folder

Place local Windows `.iso` files in this directory. ClassicSetup lists regular
files with a case-insensitive `.iso` extension on the Windows source page.
The original files are opened read-only for verification and are never moved
into or deleted with the temporary download workspace.
